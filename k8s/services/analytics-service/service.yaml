apiVersion: v1
kind: Service
metadata:
  name: analytics-service
  namespace: miniproject
spec:
  selector:
    app: analytics-service
  ports:
    - protocol: TCP
      port: 3008
      targetPort: 3008
  type: ClusterIP
