apiVersion: v1
kind: Service
metadata:
  name: feed-service
  namespace: miniproject
spec:
  selector:
    app: feed-service
  ports:
    - protocol: TCP
      port: 3002
      targetPort: 3002
  type: ClusterIP
