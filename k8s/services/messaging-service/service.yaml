apiVersion: v1
kind: Service
metadata:
  name: messaging-service
  namespace: miniproject
spec:
  selector:
    app: messaging-service
  ports:
    - protocol: TCP
      port: 3005
      targetPort: 3005
  type: ClusterIP
