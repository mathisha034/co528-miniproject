apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - namespace.yaml
  - network-policy.yaml
  - secrets/
  - infra/
  - services/
  # - certificate.yaml  # requires cert-manager CRD — skip for local dev
  - ingress.yaml
  - auth-ingress.yaml
  - minio-ingress.yaml
