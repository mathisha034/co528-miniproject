# Headless service — required for StatefulSet DNS (keycloak-0.keycloak.miniproject...)
apiVersion: v1
kind: Service
metadata:
  name: keycloak
  namespace: miniproject
spec:
  clusterIP: None
  selector:
    app: keycloak
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
---
# ClusterIP service — used by NGINX Ingress for /auth routing
apiVersion: v1
kind: Service
metadata:
  name: keycloak-http
  namespace: miniproject
spec:
  selector:
    app: keycloak
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: keycloak
  namespace: miniproject
spec:
  serviceName: "keycloak"
  replicas: 1
  selector:
    matchLabels:
      app: keycloak
  template:
    metadata:
      labels:
        app: keycloak
    spec:
      containers:
        - name: keycloak
          image: quay.io/keycloak/keycloak:23.0.7
          args: ["start-dev", "--http-relative-path=/auth", "--hostname=miniproject.local", "--proxy=edge"]
          ports:
            - containerPort: 8080
              name: keycloak
          envFrom:
            - secretRef:
                name: keycloak-secret
